rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny
    match /{document=**} {
      allow read, write: if false;
    }

    // Centralized church CMS database collections
    match /church_db/{document} {
      allow read, write: if true;
    }
  }
}
